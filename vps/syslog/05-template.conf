# One-line format handed to the omprog handler: enough context to act on, short
# enough to read on a phone lock screen.
template(name="NotificationHubFormat" type="string"
         string="%HOSTNAME% %syslogseverity-text%/%syslogfacility-text% %syslogtag%%msg%\n")
