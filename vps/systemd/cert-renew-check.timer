[Unit]
Description=Check certificate expiry every 6 hours

[Timer]
OnBootSec=15min
OnUnitActiveSec=6h
# Spread the check off the hour so it does not collide with everyone else's
# ACME traffic.
RandomizedDelaySec=20min
Persistent=true

[Install]
WantedBy=timers.target
